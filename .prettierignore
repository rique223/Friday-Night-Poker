# Dependencies and lockfiles
node_modules/
yarn.lock

# Build output
dist/
*.tsbuildinfo

# Generated — rewritten by `yarn openapi`
shared/openapi.json

# The review document: reflowing its tables and code blocks would churn the diff
QUESTIONS.md

# Environment and data
.env
.env.*
server/data/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Q99: config files used to be excluded here, which is why client/eslint.config.js was
# 2-space while the rest of the repo is 4-space. They are formatted like everything else.
